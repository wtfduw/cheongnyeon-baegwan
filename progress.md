# 작업 진행 기록

> 마지막 업데이트: 2026-08-12
> 새 세션을 시작할 때 이 파일부터 읽고, 끝날 때 "최근 작업한 것"에 오늘 한 일을 추가하세요.

## 사이트 정보
- `index.html` 상단 주석 기준 최신 버전: v12 (2026-08-12)
- 배포: GitHub Pages, 커스텀 도메인 연결 (`CNAME`)
- 백엔드 없음, 폼 없음 — 정적 사이트
- 현재 페이지 수: **1개** (`index.html` 단일 페이지, CSS도 이 파일 안에 인라인)

## 업체 정보 (고칠 때 반드시 네이버 플레이스와 동일하게)
- 상호: 청년배관설비 / 대표: 염진웅 / 사업자등록번호: 725-01-04155
- 전화: 010-6872-8284
- 주소: 전남광주통합특별시 북구 동운로201번길 20 (우편번호 61258)
- 네이버 플레이스 ID: `2043397832` → https://map.naver.com/p/entry/place/2043397832
  - `naver.me/...` 단축주소는 공유할 때마다 새로 발급됨. 코드에는 위 원래 주소를 쓸 것
- 블로그: https://blog.naver.com/tturobro / 인스타: https://www.instagram.com/ddurobro

## 행정구역 변경 (2026-07-01)
- 광주광역시 + 전라남도 → **전남광주통합특별시** (약칭 광주특별시)로 통합, "광주광역시" 명칭 폐지
- 전라북도 → 전북특별자치도 (2024-01)
- **원칙**: 구조화 데이터(JSON-LD)에는 정식 명칭을 쓰고, 화면에 보이는 제목·본문은 손님이 실제로 검색하는 "광주", "여수" 같은 말을 그대로 둔다

## 최근 작업한 것

### 2026-08-12 — CSS 분리 + 라이트박스 버그 수정 (v13) — 커밋 `791c7bd`, push·실서비스 반영 확인 완료
- **CSS를 `style.css`로 분리**: `index.html` 999줄 → 718줄, `style.css` 282줄 신설
  - 앞으로 새 페이지는 `<link rel="stylesheet" href="style.css">` 한 줄만 넣으면 같은 디자인이 적용됨
  - 색상·폰트 등을 바꿀 때 `style.css` 한 곳만 고치면 전체 페이지에 반영됨
- **팝업 안 사진이 확대되지 않던 버그 수정** (원래부터 있던 버그, CSS 분리와 무관)
  - 원인: `index.html`의 `<div class="modal-box" onclick="event.stopPropagation()">`가 클릭 신호를 막아서, 문서 전체에 걸어둔 라이트박스 리스너까지 신호가 도달하지 못했음
  - 서비스 섹션 사진은 팝업 밖이라 정상 작동했고, 헤더·모바일메뉴에서 뜨는 팝업 안 사진만 반응이 없었음
  - 해결: 라이트박스 클릭 리스너를 **캡처 단계**로 변경 (`addEventListener('click', ..., true)`). 기존 `stopPropagation()`은 그대로 둠 — 흰 상자를 눌러도 팝업이 안 닫히는 동작을 계속 유지해야 하므로
  - **교훈**: 이 `stopPropagation()`은 팝업 안에서 document 단위 이벤트를 쓰는 코드를 전부 막는다. 앞으로 팝업 안에 클릭 계측 등을 붙일 때 같은 문제가 재발할 수 있으니 주의
- 히어로 슬라이드쇼 사진은 클래스가 `.slide-photo`라 원래부터 확대 대상이 아님 (원하면 나중에 추가 가능)

### 2026-08-12 — 주소 수정 + 플레이스 연결 (v12) — 커밋 `e47a6eb`, push 완료
- **사업장 주소 수정** (`index.html` JSON-LD): `서구 상무대로 123, 4층 401호 / 61949` → `북구 동운로201번길 20 / 61258`
  - 기존 주소는 실제 사업장과 다른 곳이었음
- **행정통합 반영**: `areaServed`를 `광주광역시 + 전라남도 + 전라북도`(3개) → `전남광주통합특별시 + 전북특별자치도`(2개)로 정리
- **네이버 플레이스 연결 추가**: `sameAs`에 플레이스 주소 추가 + `hasMap` 항목 신설
  - 플레이스 → 홈페이지 연결은 원래 되어 있었으나, 홈페이지 → 플레이스는 빠져 있었음
- 화면에 보이는 부분은 변경 없음. JSON 문법 검사 통과

### 2026-08-10
- 인스타그램 링크를 기존 계정(`drain365pro`)에서 새 계정(`ddurobro`)으로 변경 (`index.html` 3곳: JSON-LD, 헤더 아이콘, CTA 버튼) — 커밋 `8e16bac`, push 완료
- `CLAUDE.md`, `progress.md`(이 파일) 신설

## 노출 확대 전략 (2026-08-12 합의)

네이버 광고만으로는 노출에 한계가 있어서 논의한 결과. 주 고객층은 40~60대(식당 자영업자, 주부)라 네이버 의존도가 높다.

**기각한 안: 홈페이지를 10개 만들기**
- 같은 사업자·전화번호·사진으로 사이트를 여러 개 만들면 네이버·구글 모두 관문 페이지(doorway page)로 보고 저품질 처리한다
- 전화번호가 같으면 한 덩어리로 묶여서, 지금 잘 돌아가는 원본 사이트까지 같이 끌려 내려갈 위험이 있다
- 애초에 네이버 검색결과에서 "웹사이트" 영역은 파이가 작다 (파워링크 > 플레이스 > VIEW > 지식iN > 웹사이트 순)

**채택한 방향: 사이트는 1개, 그 안의 페이지를 10개 이상으로**
- 효과 순위: ① 네이버 플레이스(리뷰·사진·소식) ② 사이트 페이지 늘리기 ③ 블로그 글 수 늘리기 ④ 지식iN
- 블로그도 계정을 늘리지 않는다. 1개에 글을 많이 쓴다 (새 계정은 신뢰도를 처음부터 다시 쌓아야 함)
- **중요**: 페이지를 늘리되 내용을 복붙하고 지역명만 바꾸면 사이트 10개와 똑같은 제재를 받는다. 각 페이지에 실제로 다른 내용(그 지역 시공사례, 그 증상의 원인·자가진단·비용)이 들어가야 하며, 여기엔 사장님의 실제 경험이 필요하다

## 다음에 할 것 (진행 중 — 다음 세션에서 바로 이어가기)

- ~~B-1. CSS를 `style.css`로 분리~~ ✅ 2026-08-12 완료
- **[다음 차례] B-2. 시범 페이지 1개 제작 — `/toilet/` 변기막힘**
  - 사진(`변기막힘1_전.jpg` / `변기막힘1_후.jpg`)이 이미 있고 문의가 가장 많은 항목이라 첫 페이지로 선택
  - 만들고 나서 네이버 서치어드바이저에 수집 요청 → 실제 색인되는지 확인한 뒤 나머지 확장
  - **사장님께 받아야 할 내용** (이게 곧 페이지 본문이 됨. 다른 업체엔 없는 내용이라야 검색에서 살아남음):
    1. 변기 막히는 실제 원인, 많은 순서대로 3가지
    2. 손님이 뚫어뻥으로 해결되는 경우 / 절대 안 되는 경우 구분법
    3. 실제 출동 비용대 (플레이스 기준 5~10만원, 어떨 때 위쪽인지)
    4. 손님들이 전화로 제일 많이 묻는 질문
- **C. 페이지 확장 (B-2 이후)** — 증상별 6개 `/toilet/ /sink/ /drain/ /jet/ /leak/ /faucet/`, 지역별 5개 `/gwangju/ /yeosu/ /suncheon/ /mokpo/ /jeonju/`
  - 각 페이지에 고유 title·description·h1·본문·사진·JSON-LD 필요
  - 끝나면 `sitemap.xml`에 전부 등록 (현재 URL 1개뿐) + 네이버 서치어드바이저에 수집 요청
- **D. 지식iN 감지·알림 프로그램**
  - 네이버 개발자센터 오픈API(지식iN 검색, 무료 일 25,000회)로 키워드 주기 검색 → 새 질문이면 알림 + AI 답변 초안 전달
  - **답변 등록은 반드시 사람이 직접 한다.** 자동 등록은 네이버 약관상 매크로로 간주되어 계정 정지 위험 (블로그·플레이스까지 위험)
  - 필요한 것: 네이버 개발자센터 앱 등록 후 Client ID/Secret 발급
- **[보류] 새 사업자 연결 방식 결정**
  - 상황: 실제 운영자는 1명, 사업자 등록은 2개(기존 청년배관=광주·전라 전체 / 신규=전라 지역 메인 마케팅, 실제 서비스 지역은 둘 다 동일)
  - 사업자가 실제로 2개라서 별도 사이트가 성립하는 케이스 — 위의 "홈페이지 10개 기각"과는 다른 얘기
  - 필요한 정보: 신규 사업자 이름, 전화번호, 도메인 보유 여부, 사진 공유 여부
- **[보류] FAQ 본문의 옛 지명** — `index.html` 546줄에 `광주광역시 / 전라남도 / 전라북도`가 남아 있음. "광주광역시"도 여전히 검색되는 말이라 급하지 않다고 판단해 그대로 둠. C 작업(지역 페이지) 할 때 함께 정리
- 이미지 파일 용량 최적화 검토 (현재 사진 총 3.6MB, 방문자 로딩 속도 개선용. GitHub Pages는 용량 제한 문제는 없음, 급한 건 아님)
- 유튜브 채널 개설 후 링크 추가 예정 (아직 채널 없음)

## 막힌 것

- 지금 특별히 막힌 것은 없음
