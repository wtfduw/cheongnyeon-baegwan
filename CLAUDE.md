# CLAUDE.md

## 이 저장소에 대해
- 청년배관(광주·전라 지역 배관/막힘 출동 서비스) 업체의 실제 운영 중인 홈페이지 저장소다.
- `index.html` 하나에 HTML/CSS/JS가 거의 다 들어있는 단일 파일 구조다. 서버·데이터베이스 없는 정적 사이트다.
- GitHub Pages로 호스팅되고, 커스텀 도메인(`CNAME` 파일)에 연결되어 있다. **`main` 브랜치에 push하면 곧바로 실제 웹사이트에 반영된다.**
- `README.md`: 저장소 설명 (현재 거의 비어있음)
- `progress.md`: 최근 작업한 것 / 다음에 할 것 / 막힌 것 기록

## 사용자에 대해
- 개발 경험이 없다. 터미널과 명령줄에 익숙하지 않다.
- 한국어로 대화한다.
- 이 사이트는 실제 고객을 상대하는 사업체 홈페이지다. 실수가 생기면 실제 방문자·매출에 영향을 줄 수 있으므로 연습용 저장소보다 더 신중하게 접근한다.

## 답변 방식
- 반드시 한국어로 답한다.
- 새로 나오는 영어 용어나 명령어는 뜻을 풀어서 설명한다.
- 명령어를 알려줄 때는 그 명령어가 무엇을 하는지 먼저 설명한다.
- 짧게 요약하기보다 이해할 수 있게 설명한다.

## 작업 방식
- 파일을 수정하기 전에 무엇을 어떻게 바꿀지 먼저 설명하고 확인을 받는다.
- 여러 파일을 한꺼번에 바꾸지 말고 하나씩 진행한다.
- push하면 바로 실제 사이트에 반영되므로, 특히 `index.html`을 고칠 때는 변경 내용을 신중하게 확인한 뒤 진행한다.
- 파일이나 폴더 이름은 영문 소문자, 띄어쓰기 없이 짓는다. 파일 내용은 한국어로 써도 된다.
- 작업을 시작할 때 progress.md를 읽고 상황을 파악한다. 작업이 끝나면 무엇을 했는지 progress.md에 추가한다.

## Git 규칙
- 작업을 시작할 때 `git pull`을 했는지 확인하고, 안 했으면 알려준다.
- 작업이 끝나면 `git add`, `git commit`, `git push`를 하라고 알려준다.
- 커밋 메시지는 한국어로, 무엇을 왜 바꿨는지 알아볼 수 있게 쓴다.
- 성격이 다른 작업은 커밋을 나눈다.
- 사용자가 직접 실행할 수 있도록 명령어를 알려주되, 대신 실행하지는 않는다.

## 환경
- Windows, PowerShell
- 작업 폴더: `C:\projects\cheongnyeon-baegwan`
- 배포: GitHub Pages (`main` 브랜치 push 시 자동 반영), 커스텀 도메인 연결됨
